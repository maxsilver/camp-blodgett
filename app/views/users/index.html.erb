<h1>Users</h1>

<%= link_to 'Create New User', new_user_path %>

<table>
  <thead>
    <tr>
      <th>Email</th>
      </tr>
    </thead>
<tbody>
  <% @users.each do |user| %>
  <tr>
    <td><%= link_to user.email, user_path(user) %></td>
    </tr>
  <% end %>
</tbody>
</table>
